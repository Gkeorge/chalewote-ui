import { ForbiddenEmailDirective } from './forbidden-email.directive';

describe('ForbiddenEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenEmailDirective();
    expect(directive).toBeTruthy();
  });
});
